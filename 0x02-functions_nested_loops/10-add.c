#include "main.h"
/**
 * add - adds two integer and return the result
 * @a: integr one
 * @b: integr two
 * Return: result
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
