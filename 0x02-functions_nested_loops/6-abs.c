#include "main.h"
/**
 * _abs - a function that computes the absolute value of an integer
 *
 * @c: the argument of the functiuon
 * Return: idk 9
 */
int _abs(int c)
{
	if (c > 0 || c == 0)
	{
		return (c);
	}
	else
	{
		return (c * (-1));
	}
}
