#include <stdio.h>
/**
 * main - main function level
 *
 * description: just dont know
 * the progtrtam prints " i dont care"
 * Return: 0
 */
int main(void)
{
	int num;

	num = 122;
	while (num >= 97)
	{
		putchar(num);
		num--;
	}
	putchar(10);
	return (0);
}
