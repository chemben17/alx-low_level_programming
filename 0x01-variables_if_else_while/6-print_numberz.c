#include <stdio.h>
/**
 * main - main function level
 *
 * description: like i care
 * the program prints "why do you cre"
 * Return: 0
 */
int main(void)
{
	int num;

	num = 48;
	while (num <= 57)
	{
		putchar(num);
		num++;
	}
	putchar(10);
	return (0);
}
