#include <stdio.h>
/**
 * main - main is goof
 *
 * description: like i care
 * the program prints "elo ku"
 * Return: 0
 */
int main(void)
{
	char num;

	for (num = '0'; num <= '9'; num++)
	{
		putchar(num);
	}
	for (num = 'a'; num <= 'f'; num++)
	{
		putchar(num);
	}
	putchar(10);
	return (0);
}
